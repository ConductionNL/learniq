<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
    <id>learniq</id>
    <name lang="en">Learniq</name>
    <name lang="nl">Learniq</name>
    <!-- The App Store schema caps <summary> at 128 characters. Keep both under it. -->
    <summary lang="en">Learning system for schools, training providers and companies: courses, progress, certification, compliance.</summary>
    <summary lang="nl">Leersysteem voor scholen, opleiders en bedrijven: cursussen, voortgang, certificering en compliance.</summary>
    <description lang="en"><![CDATA[Learniq is a learning system for Nextcloud: a leerlingvolgsysteem (LVS)-style progress-tracking core for primary, secondary, and higher education, and a leeromgeving / LMS for corporate L&D and government employee training — the same underlying data model serves both.

**v0.1 wedge — Compliance training**

- Annual AVG / BIO / NIS2 / Cyberbeveiligingswet refresher courses
- Bulk-enrol all employees in mandatory training
- Capture signed attestations per learner
- Detect upcoming certificate expiries
- Real-time coverage percentage per regulation
- Audit-pack export with immutable evidence log
- External-training bulk recording with officer verification and linked credential issuance
- School-year rollover wizard (propose mapping, side-effect-free preview)
- IMS QTI 2.x / 3.0 and Common Cartridge item-bank import
- Open Badges 3.0 credential issuance and public verification endpoint

**Later phases**

- K-12 leerlingvolgsysteem (Phase 2): BRON/ROD, UWLR, OSO, Edukoppeling, SchoolID/ECK iD pseudonymisation, OPP cycle, PTA grading, DigiD parent flow — a generic data-exchange job queue with an OSO parent-approval gate already exists, but no BRON/ROD, OSO, or SURFconext OpenConnector adapter is wired yet, so no wire-level exchange runs today
- Higher education (Phase 2): SURFconext federation, Studielink enrolment, OOAPI 5.0 catalog, eduPersonAffiliation, edubadges
- Assessment authoring (Phase 3): proctored exams (ProctoringProviderInterface for ProctorU, Honorlock, ExamSoft — interface only, no provider wired), EDCI verifiable credentials
- cmi5/xAPI learning-record ingest: the xAPI-statement schema and its lifecycle hook exist, but there is no learner-facing LRS ingest endpoint yet and cmi5 AU launch-token minting is a documented, currently-disabled stub (tracked in openspec/changes/cmi5-xapi-lrs-ingest/)
- AI-assisted features (Phase 5): adaptive learning paths + AI item generation, gated behind EU AI Act high-risk feature flags

**Technical foundations**

- Built on OpenRegister (data layer), OpenConnector (external system adapters), and @conduction/nextcloud-vue
- Content runtime target: cmi5 + xAPI primary, SCORM 1.2 / 2004 compatibility shim (learner-facing ingest and cmi5 launch not yet wired — see Later phases)
- Immutable audit trail backs compliance evidence, AVG record-of-processing, and AI Act decision traces
- Standards-first: 26 standards mapped (SCORM, xAPI, cmi5, LTI, QTI, EDCI, Open Badges 3.0, UWLR, OSO, ROD, Edukoppeling, SchoolID, ECK iD, SURFconext, AVG, EU AI Act, BIO2, NIS2) — most as design target, not all yet implemented

Free and open source under the EUPL-1.2 license.

**Support:** support@conduction.nl
    ]]></description>
    <description lang="nl"><![CDATA[Learniq is een leersysteem voor Nextcloud: een leerlingvolgsysteem (LVS) voor primair, voortgezet en hoger onderwijs, en een leeromgeving / LMS voor corporate L&D en overheidstrainingen — hetzelfde onderliggende datamodel bedient beide.

**v0.1 wedge — Compliance-training**

- Jaarlijkse AVG / BIO / NIS2 / Cyberbeveiligingswet herhalingstraining
- Bulk-inschrijving van alle medewerkers in verplichte trainingen
- Ondertekende attestaties per leerling vastleggen
- Detectie van aankomende verloopdata van certificaten
- Realtime dekkingspercentage per regelgeving
- Audit-pack export met onveranderlijk evidence-logboek
- Bulkregistratie van extern gevolgde trainingen, met officiersverificatie en gekoppelde certificaatuitgifte
- Wizard voor schooljaarovergang (voorstel + side-effect-vrije preview)
- Import van IMS QTI 2.x/3.0- en Common Cartridge-itembanken
- Uitgifte en publieke verificatie van Open Badges 3.0-credentials

**Latere fasen**

- K-12 leerlingvolgsysteem (Fase 2): BRON/ROD, UWLR, OSO, Edukoppeling, SchoolID/ECK iD-pseudonimisatie, OPP-cyclus, PTA-beoordeling, DigiD-ouderflow — er bestaat al een generieke data-uitwisselingswachtrij met een OSO-ouderakkoordstap, maar er is nog geen BRON/ROD-, OSO- of SURFconext-OpenConnector-adapter gekoppeld, dus er loopt nog geen daadwerkelijke uitwisseling
- Hoger onderwijs (Fase 2): SURFconext-federatie, Studielink-inschrijving, OOAPI 5.0-catalogus, eduPersonAffiliation, edubadges
- Toetsing (Fase 3): proctored exams (alleen interface, nog geen gekoppelde provider), EDCI verifieerbare credentials
- cmi5/xAPI-leerregistratie: het xAPI-statement-schema en de bijbehorende lifecycle-hook bestaan, maar er is nog geen leerling-gerichte LRS-ingest-endpoint en het aanmaken van cmi5-launchtokens is een gedocumenteerde, momenteel uitgeschakelde stub (zie openspec/changes/cmi5-xapi-lrs-ingest/)
- AI-functies (Fase 5): adaptieve leerpaden, gegate achter EU AI Act high-risk feature flags

Vrij en open source onder de EUPL-1.2-licentie.

**Ondersteuning:** support@conduction.nl
    ]]></description>
    <version>0.1.1-unstable.20260821112740</version>
    <licence>EUPL-1.2</licence>
    <author mail="info@conduction.nl" homepage="https://www.conduction.nl/">Conduction</author>
    <namespace>Learniq</namespace>
    <documentation>
        <user>https://codeberg.org/Conduction/nextcloud-scholiq</user>
        <admin>https://codeberg.org/Conduction/nextcloud-scholiq</admin>
        <developer>https://codeberg.org/Conduction/nextcloud-scholiq</developer>
    </documentation>
    <category>tools</category>
    <website>https://codeberg.org/Conduction/nextcloud-scholiq</website>
    <discussion>https://codeberg.org/Conduction/nextcloud-scholiq/discussions</discussion>
    <bugs>https://codeberg.org/Conduction/nextcloud-scholiq/issues</bugs>
    <repository>https://codeberg.org/Conduction/nextcloud-scholiq</repository>

    <screenshot>https://codeberg.org/Conduction/scholiq/raw/branch/main/img/app-store.svg</screenshot>

    <!--
        AVG Art. 30 verwerkingsregister consumes OpenRegister's per-access
        read-logging capability (the x-openregister-processing dialect,
        ProcessingLogService / ProcessingLogController, and the
        /api/avg/verwerkingen[/betrokkene] endpoints). That capability ships
        from OpenRegister >= 0.2.14. The minimum is also recorded as the
        `openregister` constraint in lib/Settings/learniq_register.json.
    -->
    <dependencies>
        <!--
        App dependencies: openregister and openconnector. Recorded as prose, not
        as elements. The App Store schema (info.xsd) has no app child under
        dependencies, so shipping one fails App Store validation; Nextcloud's
        DependencyAnalyzer does not read it either (it handles only php,
        database, command, lib, os, architecture and the server version), so the
        elements never enforced anything at install time. The openregister
        version floor is recorded as the `openregister` constraint in
        lib/Settings/learniq_register.json, as noted above.
        -->
        <php min-version="8.3" max-version="8.4"/>
        <nextcloud min-version="32" max-version="34"/>
    </dependencies>

    <background-jobs>
        <!--
            LTI AGS grade-passback poll (lti-tool-placement, design.md D2):
            pulls pending nl.conduction.lti.ags.score.received CloudEvent
            messages from OpenConnector's events-cloudevents pull surface and
            creates concept GradeEntry rows. No-ops until an admin bootstraps
            learniq.lti_ags_subscription_id (see docs/ARCHITECTURE.md).
        -->
        <job>OCA\Learniq\Cron\LtiAgsScorePollJob</job>
    </background-jobs>

    <repair-steps>
        <!--
            rename-to-learniq ORDERING (both blocks below): RenameRegisterSlug
            and MigrateAppConfigKeys MUST run BEFORE InitializeSettings.

            Renaming the app's <id> means Nextcloud discovers 'learniq' as a
            DIFFERENT app id than 'scholiq' — there is no NC concept of an
            in-place app-id upgrade, so <install> (not only <post-migration>)
            is the realistic path this actually runs through on a real
            deployment. InitializeSettings imports learniq_register.json,
            whose register entry is now keyed/slugged 'learniq'.
            OpenRegister's import handler matches an existing register BY
            SLUG (ImportHandler::importRegister() — "Check if register
            already exists by slug", falling back to "not found, will create
            new one"). If InitializeSettings ran first, it would find no
            register with slug 'learniq' yet (the real one still has the
            old slug 'scholiq' from before this rename), create a NEW EMPTY
            register with slug 'learniq', and RenameRegisterSlug's collision
            guard would then refuse to rename the real, data-carrying
            register — orphaning every existing object under the old,
            now-unreachable slug. Running RenameRegisterSlug first makes the
            real register's slug 'learniq' BEFORE the import runs, so the
            import matches and updates it in place instead of creating a
            duplicate.
        -->
        <post-migration>
            <!--
                rename-to-learniq: single-row UPDATE renaming the register's
                slug scholiq -> learniq. Idempotent, non-destructive, does not
                touch any shard table (shard tables key on the numeric
                register id, not the slug). MUST ship in the same release as
                every literal register-slug call site (see the code changes
                landed alongside this step) — data and code move together.
                MUST run before InitializeSettings — see the ordering note
                above.
            -->
            <step>OCA\Learniq\Repair\RenameRegisterSlug</step>
            <!--
                rename-to-learniq: copies (never deletes) every non-empty
                IAppConfig value stored under the old 'scholiq' app-config
                namespace to the new 'learniq' namespace, including the
                ADR-023 scholiq.actions action-authorization matrix. Idempotent
                (skips a key whose learniq.* destination is already set).
            -->
            <step>OCA\Learniq\Repair\MigrateAppConfigKeys</step>
            <step>OCA\Learniq\Repair\InitializeSettings</step>
            <!-- ADR-023: seed the action-authorization matrix (default admin-only). -->
            <step>OCA\Learniq\Repair\InitializeActions</step>
            <!--
                Must run AFTER the register sync that adds the English columns.
                Covers every renamed property together: a register-scoped step
                that moved some owners and not others would leave the rest
                silently reading null.
            -->
            <step>OCA\Learniq\Repair\RenameDutchColumns</step>
        </post-migration>
        <install>
            <!--
                rename-to-learniq: registered under <install> too, not only
                <post-migration> — see the ordering note above for why a
                fresh 'learniq' install is the realistic path on a real
                deployment. Both steps are idempotent and safe to run here:
                on a genuinely fresh instance with no prior 'scholiq' data
                they are no-ops (nothing to rename, nothing to copy).
            -->
            <step>OCA\Learniq\Repair\RenameRegisterSlug</step>
            <step>OCA\Learniq\Repair\MigrateAppConfigKeys</step>
            <step>OCA\Learniq\Repair\InitializeSettings</step>
            <!-- ADR-023: seed the action-authorization matrix (default admin-only). -->
            <step>OCA\Learniq\Repair\InitializeActions</step>
        </install>
    </repair-steps>

    <settings>
        <admin>OCA\Learniq\Settings\AdminSettings</admin>
        <admin-section>OCA\Learniq\Sections\SettingsSection</admin-section>
    </settings>

    <navigations>
        <navigation>
            <id>learniq</id>
            <name>Learniq</name>
            <route>learniq.page.index</route>
            <icon>app.svg</icon>
        </navigation>
    </navigations>
</info>
